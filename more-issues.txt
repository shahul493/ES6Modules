UI Issues:

1. aria-atomic="true" for the calculators where aira-live="polite" used
2. info-icon - has popup
3. skiptomain - focus
4. callback for fadein animtions with aria-live="off"	
       $("p").fadeOut(3000, function(){
            alert("fadeOut() method is finished!");
        });
5. remove role="buttons" and alt

